# Dev cost exception

Non-production environments may set `single_nat_gateway = true` to reduce NAT
gateway hourly charges while preserving three-AZ subnet layout for parity with
staging and production.
